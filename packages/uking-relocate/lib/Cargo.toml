[package]
name = "uking-relocate-lib"
version = "0.0.0"
edition = "2021"
publish = false
description = "Library for uking-relocate"

[dependencies]
serde = { version = "1.0.217", features = ["derive"] }

[features]
loader-data = [] # Enable static data used by the CLI tool
